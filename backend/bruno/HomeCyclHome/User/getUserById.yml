info:
  name: getUserById
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/users/:id
  params:
    - name: id
      value: '1'
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
