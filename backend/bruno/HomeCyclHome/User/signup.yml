info:
  name: signup
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/auth/signup
  body:
    type: json
    data: |-
      {
        "email": "pierredepiets@hotmail.fr",
        "password": "pierre64",
        "firstName": "Pierre",
        "lastName": "Laffaille"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
